    <section>
      <div class="container">
        <div class="row">
            {{#each products}}
          <div class="col-md-3">

            <div class="card" style="width: 18rem;">
              <img src="{{imageLink}}" class="card-img-top img-products" alt="..." />
             
              <div class="card-body">
                <h5 class="card-title">{{name}}</h5>
                <p class="card-text">{{discription}}</p>
                <a href="#" class="btn btn-primary">Add to cart</a>
              </div>
            </div>

          </div>
          {{/each}}
        </div>
      </div>
    </section>